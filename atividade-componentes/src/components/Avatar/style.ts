import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        width: 64,
        height: 64,
        borderRadius: 100,
        backgroundColor: "#016200"
    }
});